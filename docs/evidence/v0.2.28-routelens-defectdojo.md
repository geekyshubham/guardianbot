# v0.2.28 RouteLens DefectDojo workflow evidence

Verified: 2026-07-29

This evidence verifies the production workflow correction for DefectDojo's
`ZAP Scan` parser. It does not claim that authenticated-full nightly scanning,
least-privilege DefectDojo permissions, or every installed repository is
complete.

## Immutable GuardianBot release

- Release: [GuardianBot v0.2.28](https://github.com/geekyshubham/guardianbot/releases/tag/v0.2.28)
- Source commit: `10e9b42cc42f73ff7c307d0415adb9ec26c3916a`
- Release run: `30480720277`
- Signed control-plane image:
  `ghcr.io/geekyshubham/guardianbot@sha256:b3f3f4d087f2418c4ec298f86e99ec4c310dfed23bbb94c3294e4f2954fb55c8`
- Active DigitalOcean control-plane deployment:
  `fb9f8f64-13b8-47d4-92a5-8ae20c2a5185`

The release workflow passed immutable source verification, the complete
repository gate, Critical image scanning, CycloneDX generation, GitHub
provenance verification, keyless signing, SBOM attestation, and signed release
manifest verification. The active DigitalOcean deployment uses the exact
release digest, has all three trusted reusable-workflow SHAs set to the release
commit, and returned HTTP 200 from `/healthz` and `/readyz`.

## Generic repository upgrade

`guardianctl upgrade geekyshubham/RouteLens` created draft pull request
[#69](https://github.com/geekyshubham/RouteLens/pull/69). Its only changes were
the declarative `workflowVersion` and four reusable-workflow pins. Application,
scanner, image-build, runtime-smoke, Trivy, and SBOM checks passed before merge.

The first scanner attempt correctly failed evidence attestation because only
the DAST trust SHA had been rotated. GuardianBot then rotated the security,
image, and DAST allowlist SHAs atomically to the same v0.2.28 commit and
re-ran the failed checks successfully. No consumer secret or scanner logic was
changed.

The merge commit is `f256e7e18b862bfa0567632b6b45ec9f92f99b52`.
Default-branch run `30481642550` tested, scanned, generated an SBOM, signed, and
promoted RouteLens digest
`sha256:907d25e90c047d3f690043802401ba2cc87786e1c931ec13ecbb9a913e9d36f6`.
DigitalOcean deployment `270ba8ad-c58f-44e6-b69b-efd0e0f595d3` made that exact
digest active for the RouteLens service and migration job.

## Authenticated DAST artifact

Manual run `30482063609` used the merged default-branch commit and the exact
accepted deployment evidence. The control plane issued the one-time
RouteLens session, the protected assertion passed, and the bounded
authenticated-baseline ZAP job completed in 3 minutes 30 seconds.

Artifact `8736154746` contains exactly:

- `scan-status.json`
- `zap.json`
- `zap.xml`
- `provenance-manifest.json`
- `provenance-token.txt`

The provenance manifest binds the artifact to the v0.2.28 DAST workflow SHA.
It records SHA-256 hashes for both scanner reports. `zap.xml` is a well-formed
`OWASPZAPReport` of 105,761 bytes with SHA-256
`78c9b455379c85737922c0afc3d9ab4f5fb639f41717a41b090b525295482ebf`.
The status binds the scan to staging digest
`sha256:907d25e90c047d3f690043802401ba2cc87786e1c931ec13ecbb9a913e9d36f6`
and records ZAP exit code `2`, which is report-only policy evidence rather than
an operational failure.

## DefectDojo reimport

The control plane normalized GuardianBot findings from `zap.json` and submitted
the provenance-bound `zap.xml` to DefectDojo. The reimport preserved existing
ZAP Test ID `5` and updated it to:

- build ID `30482063609/1`
- commit `f256e7e18b862bfa0567632b6b45ec9f92f99b52`
- branch `main`
- 12 active scanner-native findings: 1 Medium, 6 Low, and 5 Info

This closes the live parser-format defect found by the preceding JSON reimport.
Semgrep Test `2`, filesystem Trivy Test `3`, and image Trivy Test `4` had
already demonstrated same-Test-ID workflow reimport.

## Remaining boundary

- RouteLens authenticated-full nightly evidence is not yet verified.
- AstraNull has not yet been upgraded to the JSON/XML artifact contract or
  imported into DefectDojo.
- The DefectDojo PoC automation identity still needs least-privilege hardening.
- The destructive restore drill, HA, and off-host media backup remain
  unverified.
