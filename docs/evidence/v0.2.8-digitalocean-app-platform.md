# GuardianBot v0.2.8 DigitalOcean App Platform evidence

Verified on 2026-07-27 against the production `guardianbot-prod` DigitalOcean
App Platform app.

This page records non-secret operational evidence. It intentionally excludes
GitHub credentials, database connection strings, private keys, webhook
secrets, evidence-signing secrets, and DigitalOcean tokens.

## Release identity

- Release: [GuardianBot v0.2.8](https://github.com/geekyshubham/guardianbot/releases/tag/v0.2.8)
- Release run: [GitHub Actions run 30256689065](https://github.com/geekyshubham/guardianbot/actions/runs/30256689065)
- Source commit: `359ee52824beead882330767f838a48e24766857`
- Source ref: `refs/tags/v0.2.8`
- Platform: `linux/amd64`
- Image:
  `ghcr.io/geekyshubham/guardianbot@sha256:df5a9c33dfbd134c49e9ef56d3c0487758d30d4f3596aa8e08f476f5a3fdef05`
- Builder identity:
  `https://github.com/geekyshubham/guardianbot/.github/workflows/release.yml@refs/tags/v0.2.8`

The release run passed remote annotated-tag and default-branch ancestry
verification, the full clean repository gate, the Trivy Critical-image gate,
CycloneDX SBOM generation, GitHub build provenance, exact-identity Cosign
signing, CycloneDX attestation, signed release-manifest generation, immutable
stable-tag publication, and independent release-asset verification.

Before deployment, the downloaded fixed asset allowlist and checksums were
verified locally. The release-manifest signature, exact image signature,
CycloneDX attestation, GitHub provenance, source commit/ref, and immutable GHCR
digest were then independently verified by the deployment entry point.

## DigitalOcean deployment

- App ID: `346b3b81-b8cf-4136-b706-0a7195bc9f00`
- Deployment ID: `2ed56b00-8b05-465d-8112-5f148b190385`
- Deployment state: `ACTIVE`
- Public origin:
  [guardianbot-prod-sfdme.ondigitalocean.app](https://guardianbot-prod-sfdme.ondigitalocean.app)
- The deployed service uses the exact manifest digest and has no mutable image
  tag.
- Security, image, and DAST workflow trust each use the exact release commit.
- The DigitalOcean managed PostgreSQL CA binding, encrypted operational
  secrets, one `basic-xs` instance, health check, and 15-minute reconciliation
  interval were retained.

The workflow-trust update was first deployed and health-checked independently.
The signed release deployment then promoted only the canonical GuardianBot
service image to the verified digest and re-read the active deployment spec
before reporting success.

## Live probes

| Probe | Expected | Observed |
| --- | --- | --- |
| `GET /healthz` | HTTP 200 and `{"status":"ok"}` | Passed |
| `GET /readyz` | HTTP 200 and `{"status":"ready"}` | Passed |
| `GET /metrics` without authorization | HTTP 404 | Passed |
| `POST /evidence/attest` without GitHub OIDC | HTTP 401 | Passed |
| `POST /dast/session` without GitHub OIDC | HTTP 401 | Passed |
| `POST /webhooks/github` with an invalid signature | HTTP 401 | Passed |
| Unknown route | HTTP 404 | Passed |

The active deployment spec, exact digest, and all three workflow trust pins
were independently reconciled after rollout. The inspected startup log window
contained no error, warning, failure, exception, or fatal signal.

## Remaining live work

- Consumer repositories still need successful scanner evidence against this
  workflow SHA before the scanner capability can move beyond beta.
- The new GitHub App still requires explicit operator confirmation before
  creation, private-key generation, and installation.
- No production model-bridge credential is configured; deterministic review
  and scanning rollout does not depend on it.
- RouteLens and AstraNull image promotion, isolated staging, authenticated ZAP,
  and DefectDojo evidence remain to be completed through the generic onboarding
  path.
