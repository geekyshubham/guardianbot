# GuardianBot v0.2.11 DigitalOcean App Platform evidence

Verified on 2026-07-28 against the production `guardianbot-prod` DigitalOcean
App Platform app.

This page records non-secret operational evidence. It intentionally excludes
GitHub credentials, database connection strings, private keys, webhook
secrets, evidence-signing secrets, and DigitalOcean tokens.

## Release identity

- Release: [GuardianBot v0.2.11](https://github.com/geekyshubham/guardianbot/releases/tag/v0.2.11)
- Release run: [GitHub Actions run 30361659886](https://github.com/geekyshubham/guardianbot/actions/runs/30361659886)
- Source commit: `70dc78960bc4cded19c9d191c4f5615990c987f8`
- Source ref: `refs/tags/v0.2.11`
- Annotated tag object: `93da47ebab58291d009222df4b9bb177436beb2f`
- Platform: `linux/amd64`
- Image:
  `ghcr.io/geekyshubham/guardianbot@sha256:e69e9c92bd6c5e9c7a4928b6543cd144b14877c2e0ddeefac69a7212787a2f26`
- Builder identity:
  `https://github.com/geekyshubham/guardianbot/.github/workflows/release.yml@refs/tags/v0.2.11`

The release run passed authenticated remote annotated-tag verification,
checked-out SHA verification, current default-branch SHA and ancestry
verification, the full clean repository gate, the Trivy Critical-image gate,
CycloneDX SBOM generation, GitHub build provenance, exact-identity Cosign
signing, CycloneDX attestation, signed release-manifest generation, immutable
stable-tag publication, and independent release-asset verification.

Before deployment, the downloaded fixed 11-file asset allowlist and checksums
were verified locally. The release-manifest signature, exact image signature,
CycloneDX attestation, GitHub provenance, source commit/ref, and immutable GHCR
digest were independently verified. The deployment entry point repeated those
checks before changing DigitalOcean.

## DigitalOcean deployment

- App ID: `346b3b81-b8cf-4136-b706-0a7195bc9f00`
- Trust-pin deployment ID: `9693422d-22a7-473d-84c2-31246f22ef81`
- Signed-image deployment ID: `9537a99e-744c-4ffc-80bb-b60de6ae195e`
- Final deployment state: `ACTIVE`
- Public origin:
  [guardianbot-prod-sfdme.ondigitalocean.app](https://guardianbot-prod-sfdme.ondigitalocean.app)
- The active service uses the exact signed-manifest digest and has no mutable
  image tag.
- Security, image, and DAST workflow trust each occur exactly once and equal
  the release source commit.
- The trusted workflow repository remains exactly
  `geekyshubham/guardianbot`.
- Existing encrypted operational secrets and the DigitalOcean managed
  PostgreSQL binding were retained.

The workflow-trust update changed only the three expected SHA values and was
deployed and health-checked independently. The signed release deployment then
promoted only the canonical GuardianBot service image to the verified digest.
Both checks read the immutable active-deployment spec rather than relying only
on the editable app spec.

## Live probes

| Probe | Expected | Observed |
| --- | --- | --- |
| `GET /healthz` | HTTP 200 and `{"status":"ok"}` | Passed |
| `GET /readyz` | HTTP 200 and `{"status":"ready"}` | Passed |
| `GET /metrics` without authorization | HTTP 404 | Passed |
| `POST /evidence/attest` without GitHub OIDC | HTTP 401 | Passed |
| `POST /dast/session` without GitHub OIDC | HTTP 401 | Passed |
| `POST /webhooks/github` with a well-formed invalid signature | HTTP 401 | Passed |
| Unknown route | HTTP 404 | Passed |

The final active deployment spec, exact digest, workflow repository, and all
three workflow trust pins were independently reconciled after rollout.
DigitalOcean returned zero control-plane run-log lines for the inspected
deployment window. The error-pattern match count was therefore zero, but that
absence is not represented as affirmative startup-log evidence.

## Remaining live work

- Consumer repositories still need successful scanner evidence against the
  `v0.2.11` workflow SHA before the scanner capability can move beyond beta.
- The new GitHub App still requires explicit operator confirmation before
  creation, private-key generation, and installation.
- No production model-bridge credential is configured; deterministic review
  and scanning rollout does not depend on it.
- RouteLens and AstraNull have blocking image findings and do not yet have
  verified isolated staging, signed promotion, authenticated ZAP, or
  DefectDojo evidence.
