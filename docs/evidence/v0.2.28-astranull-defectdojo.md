# v0.2.28 AstraNull image, DAST, and DefectDojo evidence

Verified: 2026-07-29

This evidence verifies AstraNull through the same generic GuardianBot upgrade
and reusable workflows used by RouteLens. It does not claim that the
authenticated-full nightly profile, the seven-day report-only period, or
enforcement is complete.

## Generic upgrade and immutable image

`guardianctl upgrade geekyshubham/AstraNull` created pull request
[#7](https://github.com/geekyshubham/AstraNull/pull/7). The PR changed only the
declarative GuardianBot version and four immutable reusable-workflow pins to
GuardianBot v0.2.28 commit
`10e9b42cc42f73ff7c307d0415adb9ec26c3916a`.

The PR checks passed before merge. Merge commit
`b3989d27409bb512821b60149fe0a20f81f41b11` triggered default-branch run
[`30482719437`](https://github.com/geekyshubham/AstraNull/actions/runs/30482719437).
That run passed the deterministic security gate, repository tests, migrations,
runtime health checks, Trivy image scanning, Critical policy, CycloneDX SBOM
generation, immutable GHCR push, keyless Cosign signing, and SBOM attestation.

The promoted image is
`ghcr.io/geekyshubham/astranull@sha256:4409369b4202d69055646df4dce5b9589052edaae8da69a10ba8790db07db0a3`.
Promotion artifact `8736374844` binds the image to the v0.2.28 workflow identity
and records SBOM SHA-256
`19b0ee50bdebd69a9abc5d210dc51f33dd0a3d3484845fdc5441bf061d76fb44`.

DigitalOcean deployment `e220064c-4928-4b5b-95bb-4dc3ca74037e` reached
`ACTIVE` with that exact digest on both the `astranull` service and `migrate`
job.

## Deployment-bound authenticated DAST

Manual run
[`30483110737`](https://github.com/geekyshubham/AstraNull/actions/runs/30483110737)
used the merged commit and the accepted DigitalOcean deployment evidence. The
control plane issued a one-time session, the bounded same-origin OpenAPI and
protected assertion checks passed, and the authenticated-baseline ZAP scan
completed.

Artifact `8736511062` contains exactly:

- `scan-status.json`
- `zap.json`
- `zap.xml`
- `provenance-manifest.json`
- `provenance-token.txt`

The status binds the scan to the exact deployed digest and records ZAP exit
code `2`, meaning report-only findings were present without an operational scan
failure. The well-formed `OWASPZAPReport` XML is 29,598 bytes with SHA-256
`a50050b914aa07318a8e6f9b0bfd697a16de900472fe291fbb0a64bc9e424d39`.

## DefectDojo import and reimport

The first run created dedicated DefectDojo product
`geekyshubham/AstraNull`, DAST engagement `5`, and ZAP Test ID `6`. It imported
9 active, verified scanner-native findings: 5 Low and 4 Info, with no Medium,
High, or Critical findings.

A second independent manual run
[`30483273544`](https://github.com/geekyshubham/AstraNull/actions/runs/30483273544)
completed the same authenticated workflow. The control plane reimported into
the same Test ID `6`, updated its build ID to `30483273544/1`, retained the
same commit and branch provenance, and kept the deduplicated finding total at
9. This verifies stable reimport rather than only first-time import.

`guardianctl doctor geekyshubham/AstraNull` reports `ready`. Enforcement
readiness remains false by design: the baseline is not present and only 0.59
of the required seven report-only days has elapsed.

## Remaining boundary

- AstraNull authenticated-full nightly evidence is not yet verified.
- The seven-day report-only observation and reviewed baseline are incomplete.
- The DefectDojo PoC automation identity still needs least-privilege hardening.
- Destructive restore testing, off-host backup media, and high availability
  remain unverified.
