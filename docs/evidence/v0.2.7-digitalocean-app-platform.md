# GuardianBot v0.2.7 DigitalOcean App Platform evidence

Verified on 2026-07-27 against the production `guardianbot-prod` DigitalOcean
App Platform app.

This page records non-secret operational evidence. It intentionally excludes
GitHub credentials, database connection strings, private keys, webhook
secrets, evidence-signing secrets, and DigitalOcean tokens.

## Release identity

- Release: [GuardianBot v0.2.7](https://github.com/geekyshubham/guardianbot/releases/tag/v0.2.7)
- Release run: [GitHub Actions run 30246817497](https://github.com/geekyshubham/guardianbot/actions/runs/30246817497)
- Source commit: `24167b17e2a7f25f3edb252ab389e178b4392cfe`
- Source ref: `refs/tags/v0.2.7`
- Platform: `linux/amd64`
- Image:
  `ghcr.io/geekyshubham/guardianbot@sha256:b23640a96b81155dfd56c20a8cac8f0a3cbf61301be38e21a1c54d5f55b667ea`
- Builder identity:
  `https://github.com/geekyshubham/guardianbot/.github/workflows/release.yml@refs/tags/v0.2.7`

The release run passed the full repository gate, Trivy Critical-image gate,
CycloneDX SBOM generation, GitHub build provenance, exact-identity Cosign image
signing, CycloneDX attestation, signed release-manifest generation, immutable
stable-tag publication, and independent release-asset verification.

Before deployment, the downloaded canonical asset allowlist and checksums were
verified locally. The release-manifest signature, exact image signature,
CycloneDX attestation, GitHub provenance, source commit/ref, and both immutable
GHCR tags were then independently verified.

## DigitalOcean deployment

- App ID: `346b3b81-b8cf-4136-b706-0a7195bc9f00`
- Deployment ID: `129a518f-594d-456d-93a8-2c8b5facdbf6`
- Deployment state: `ACTIVE`
- Public origin:
  [guardianbot-prod-sfdme.ondigitalocean.app](https://guardianbot-prod-sfdme.ondigitalocean.app)
- Runtime image digest exactly matched the signed release manifest.
- The service retained one `basic-xs` instance with its `/healthz` App
  Platform health check.
- The DigitalOcean managed PostgreSQL binding used
  `GUARDIANBOT_DATABASE_CA_CERT=${guardianbot-db.CA_CERT}`.
- The evidence-signing secret remained encrypted in App Platform.
- Security, image, and DAST workflow trust were each pinned to the exact
  release commit.
- Continuous reconciliation was enabled at a 15-minute interval.

The first configuration-only deployment used the previous image, which did not
consume the managed PostgreSQL CA and exited on certificate-chain validation.
DigitalOcean automatically rolled it back to the prior healthy deployment. The
signed v0.2.7 image was then deployed with the corrected CA contract and became
active. This validates the rollback boundary as well as the final deployment.

## Live probes

The final deployment passed:

| Probe | Expected | Observed |
| --- | --- | --- |
| `GET /healthz` | HTTP 200 and `{"status":"ok"}` | Passed |
| `GET /readyz` | HTTP 200 and `{"status":"ready"}` | Passed |
| `GET /metrics` without authorization | HTTP 404 | Passed |
| `POST /evidence/attest` without GitHub OIDC | HTTP 401 | Passed |
| `POST /dast/session` without GitHub OIDC | HTTP 401 | Passed |
| `POST /webhooks/github` without a valid signature | HTTP 401 | Passed |
| Unknown route | HTTP 404 | Passed |

Startup runs PostgreSQL migrations before the HTTP server starts, and
`/readyz` performs a live store ping while also checking worker and monitoring
scheduler health. Therefore the active readiness result provides live evidence
that migrations completed, the managed-CA database connection works, and the
initial monitoring cycle completed. The final active deployment log contained
no error, warning, failure, exception, or fatal entries in the inspected
startup window.

## Remaining live work

- The new GitHub App still requires explicit operator confirmation before
  creation, private-key generation, and installation.
- No production model-bridge credential is configured; deterministic review
  and scanning rollout does not depend on it.
- RouteLens and AstraNull image promotion, isolated staging, authenticated ZAP,
  and DefectDojo evidence remain to be completed through the generic onboarding
  path.
- Dedicated-droplet deployment, restore drills, and high-availability behavior
  remain unverified.
