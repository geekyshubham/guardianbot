# GuardianBot v0.2.14 live PoC evidence

Verified on 2026-07-29 against the production `guardianbot-prod` DigitalOcean
App Platform control plane and the Geekyshubham GitHub organization.

This page records non-secret operational evidence. It excludes GitHub tokens,
DigitalOcean tokens, database connection strings, private keys, webhook
secrets, model credentials, and evidence-signing secrets.

## GuardianBot release and deployment

- Release:
  [GuardianBot v0.2.14](https://github.com/geekyshubham/guardianbot/releases/tag/v0.2.14)
- Release run:
  [GitHub Actions run 30423468517](https://github.com/geekyshubham/guardianbot/actions/runs/30423468517)
- Source commit: `4fdb24218bb7409af59e185e018c5d8b39f669de`
- Image:
  `ghcr.io/geekyshubham/guardianbot@sha256:bdd264afb6a078610242dedcfc11362b0d82c9f44cd46171fc06b868c7dbe543`
- DigitalOcean app ID: `346b3b81-b8cf-4136-b706-0a7195bc9f00`
- DigitalOcean deployment:
  `37480318-19ce-45e6-b455-a1d8c4b1304e`

The release workflow passed source validation, repository checks, Trivy image
policy, CycloneDX generation, GitHub provenance, keyless signing, SBOM
attestation, signed release-manifest generation, and release-asset
verification. DigitalOcean then promoted that exact release digest without
changing the app's database, size, or trust configuration. `GET /healthz` and
`GET /readyz` returned HTTP 200 after the deployment became active.

## Repository discovery

The production PostgreSQL store was queried from the active control-plane
instance over the existing CA-pinned DigitalOcean connection:

- repository records: 13;
- active repository records: 13;
- repositories with immutable index SHAs: 13;
- GitHub App installations represented: one.

GitHub issue search independently returned exactly 13 open issues titled
`GuardianBot onboarding inventory`, one in every installed repository.

## AstraNull image validation and promotion

- Onboarding PR:
  [AstraNull #3](https://github.com/geekyshubham/AstraNull/pull/3)
- Merged commit: `2aee0ff50d4138dcb35aaff9e35a9cfcd0f739a6`
- Main-branch GuardianBot run:
  [30423701607](https://github.com/geekyshubham/AstraNull/actions/runs/30423701607)
- Promoted image:
  `ghcr.io/geekyshubham/astranull@sha256:a6d675fea8fe6ee362c36227c38b1daba91a21e7fdda8fcd061c63f5bd3ebbd0`
- SBOM SHA-256:
  `9fd8b7b80338e1f06df3d45a784f049f8c1277fc3489cd5bb6d3a0594659150e`

The same transferred image passed repository tests, PostgreSQL migrations,
runtime health/readiness checks, Trivy scanning, CycloneDX generation, and the
Critical policy before it was pushed by digest. The promotion job then
verified the expected default-branch workflow identity, keylessly signed the
digest, attached and verified the SBOM attestation, and uploaded
control-plane-attested promotion evidence.

The repository's pre-existing DigitalOcean deployment workflow also completed,
but it rebuilds its own image. It is not evidence that the signed GuardianBot
digest was deployed to isolated staging.

## RouteLens image validation and promotion

- Onboarding PR:
  [RouteLens #60](https://github.com/geekyshubham/RouteLens/pull/60)
- Merged commit: `48b53ec875ca73fc36f5c031572c971c46ba348c`
- Main-branch GuardianBot run:
  [30423921444](https://github.com/geekyshubham/RouteLens/actions/runs/30423921444)
- Promoted image:
  `ghcr.io/geekyshubham/routelens@sha256:ed954dbddadff7b27e390786f05a4e9f4af5cb817caa2e3c661ed355b9f93332`
- SBOM SHA-256:
  `440dc793b493810d9e89514223896102028ec2136235fa11b630077a13628602`

The same transferred image passed application checks, disposable
PostgreSQL/Redis startup, migrations, `/api/v1/health/`, Trivy scanning,
CycloneDX generation, and the Critical policy before it was pushed by digest.
The promotion job then verified the expected default-branch workflow identity,
keylessly signed the digest, attached and verified the SBOM attestation, and
uploaded control-plane-attested promotion evidence.

## Isolated DigitalOcean staging

- Droplet ID: `588356593`
- Region and size: `blr1`, `s-2vcpu-4gb`
- DigitalOcean Firewall:
  `1e1d59d9-d9d1-4010-8c2f-8bcc7190e643`
- RouteLens origin:
  `https://routelens.168.144.217.141.sslip.io`
- AstraNull origin:
  `https://astranull.168.144.217.141.sslip.io`

The Droplet runs the checked [`infra/staging`](../../infra/staging/README.md)
definition. SSH is restricted to the observed operator `/32` in both the
DigitalOcean Firewall and UFW; only HTTP and HTTPS are public. fail2ban is
active, runtime secrets are root-owned mode `0600`, and no application or
database port is published.

Docker reported each running container's configured image and content ID as the
same promoted digest:

- RouteLens:
  `sha256:ed954dbddadff7b27e390786f05a4e9f4af5cb817caa2e3c661ed355b9f93332`
- AstraNull:
  `sha256:a6d675fea8fe6ee362c36227c38b1daba91a21e7fdda8fcd061c63f5bd3ebbd0`

RouteLens and AstraNull have separate `internal: true` Docker networks and
separate PostgreSQL volumes. RouteLens could not resolve the AstraNull database
service, and AstraNull could not resolve the RouteLens database service.

Public verification returned:

- RouteLens `/api/v1/health/`: healthy with PostgreSQL connected;
- RouteLens `/api/targets/`: HTTP 401 without authentication;
- RouteLens `/api/schema/`: OpenAPI 3.0.2 with 206 paths;
- AstraNull `/health`: healthy;
- AstraNull `/ready`: ready with PostgreSQL, OIDC-JWT, and signed-worker mode;
- AstraNull `/.well-known/jwks.json`: same-origin RS256 JWKS;
- AstraNull `/internal/admin/break-glass/status`: HTTP 401 without
  authentication; and
- both HTTP origins: permanent redirect to HTTPS with HSTS on HTTPS responses.

This verifies an exact-digest staging deployment and runtime isolation. It does
not yet satisfy GuardianBot's deployment-bound DAST broker because the current
automated reconciler accepts App Platform deployment evidence, not Droplet
health evidence.

## Remaining live work

- Configure deployment-bound DAST authentication and run safe authenticated
  ZAP smoke and nightly profiles.
- Deploy DefectDojo on DigitalOcean and verify import/reimport.
- Configure a production model-bridge credential and demonstrate a real
  advisory PR review.
- Complete the seven-day report-only observation period and enforcement
  readiness checks.
- Run the fresh-repository Python, Node, Swift, Ruby, Docker, and
  documentation-only acceptance matrix.
