# GuardianBot v0.2.26 AstraNull deployment-bound DAST evidence

Verified on 2026-07-29 against the production `guardianbot-prod` control plane,
Geekyshubham GitHub repositories, and isolated DigitalOcean App Platform
staging. This record excludes credentials, one-time session material, database
connection strings, private keys, and API tokens.

## GuardianBot release

- Release:
  [GuardianBot v0.2.26](https://github.com/geekyshubham/guardianbot/releases/tag/v0.2.26)
- Release run:
  [30473386620](https://github.com/geekyshubham/guardianbot/actions/runs/30473386620)
- Source and trusted reusable-workflow commit:
  `70df177cd0cbdadf97f819bcdfc13a1e16216ce5`
- Signed control-plane image:
  `ghcr.io/geekyshubham/guardianbot@sha256:7e953a8958727e8dacde6909c4b38ae42efda68840e0f2f8ea223ba706b07c67`
- DigitalOcean app:
  `346b3b81-b8cf-4136-b706-0a7195bc9f00`
- Active deployment after the exact trust-pin update:
  `537e04be-f76e-4566-b5fb-b96de7c179ad`

The release passed repository checks, Trivy policy, CycloneDX generation,
GitHub provenance, keyless signing, SBOM attestation, and signed
release-manifest verification. `/healthz` and `/readyz` returned HTTP 200 after
deployment. Security, image, and DAST workflow trust all resolve to the exact
source commit above.

## Generic AstraNull onboarding upgrade

- Generic upgrade PR:
  [AstraNull #6](https://github.com/geekyshubham/AstraNull/pull/6)
- Merge commit: `ae2e88404852add2c5ce81751d83a9d49c749ebc`
- Default-branch GuardianBot run:
  [30474859843](https://github.com/geekyshubham/AstraNull/actions/runs/30474859843)
- Signed and SBOM-attested image:
  `ghcr.io/geekyshubham/astranull@sha256:9b34a59d56427f0ad74fb2d47ad5c7924440ba7f6a4778189784a0ed0194e6ae`
- DigitalOcean staging app:
  `2a76914e-d04e-4a6c-8b9c-929a1e8976e2`
- Active exact-digest deployment:
  `19508d7a-50d9-441f-aac5-d89641dd15ef`

The generated PR pins all four reusable jobs to v0.2.26 and adds a declarative
deployment-bound DAST profile plus a repository-owned OpenAPI artifact
containing only `GET /health`, `GET /ready`, and protected
`GET /v1/checks`. AstraNull's non-secret runtime contract and names of
runner-generated ephemeral values are stored in `.guardianbot/config.yml`, so
the checked-in caller matches the central generator and future upgrades retain
the image smoke contract.

PR and default-branch runs passed deterministic scanners, exact image build,
disposable PostgreSQL/Redis startup, repository tests, migrations, health and
readiness smoke, Trivy, CycloneDX SBOM generation, zero-Critical policy, GHCR
promotion, keyless signing, and SBOM/provenance attestation. The control-plane
reconciler deployed the same digest to both the AstraNull service and migration
job. `/health` and `/ready` then returned HTTP 200; `/v1/checks` and
`POST /guardianbot/session` returned HTTP 401 without authentication.

## Authenticated DAST smoke

- Workflow-dispatch run:
  [30475277077](https://github.com/geekyshubham/AstraNull/actions/runs/30475277077)
- Authenticated DAST job:
  [90655342911](https://github.com/geekyshubham/AstraNull/actions/runs/30475277077/job/90655342911)
- Immutable artifact ID: `8733364316`
- Profile: `authenticated-baseline`
- Exact target digest:
  `sha256:9b34a59d56427f0ad74fb2d47ad5c7924440ba7f6a4778189784a0ed0194e6ae`
- ZAP exit code: `2` (advisory findings; no operational failure)
- Runtime: 1 minute 5 seconds

The job validated the exact repository ID, default-branch SHA, reusable
workflow SHA, staging environment, origin, deployment digest, and registered
session-assertion path. It required unauthenticated `/v1/checks` access to
fail, exchanged a central secret for a short-lived viewer bearer, required the
authenticated assertion to succeed, and destroyed session material after the
scan. The safe OpenAPI sanitizer retained only the three exact-origin GET
operations before the pinned, non-root ZAP image ran in passive safe mode.

The report contained nine advisory alert types: zero Critical, zero High, zero
Medium, five Low, and four Informational. Each alert type covered the three
bounded operations. The attested evidence hashes were:

- `scan-status.json`:
  `bcec254f11165aa9b74c50838de0d81ff4ff3eb2a688619577baf9ba7459700b`
- `zap.json`:
  `b7066d16d1d17b608450c1edf699d629a1ac778375b2b00d237761946d8371ac`
- `provenance-manifest.json`:
  `6d66076e1f82297e653e66f3b1fc9d8d15fd8cf056b78bc0dc845f8236f6090e`

Non-secret machine-readable counts are retained in
[`v0.2.26-astranull-dast-summary.json`](v0.2.26-astranull-dast-summary.json).

## Remaining scope

- Run and verify the `authenticated-full` nightly active-scan profile.
- Deploy DefectDojo on DigitalOcean and verify import/reimport.
- Triage Low and Informational response-header and cookie findings; DAST
  findings remain advisory under the PoC policy.
