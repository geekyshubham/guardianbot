# DAST authentication failure

Stop before active scanning. Verify the exact staging origin, secret reference,
login response, and protected session assertion without logging credentials.
Rotate the staging credential if exposure is possible. Never downgrade to an
unauthenticated scan and claim authenticated coverage.
