# DefectDojo import failure

Preserve raw artifacts and import metadata, verify DigitalOcean-private
reachability and the central token, then reimport the same logical test. Confirm
deduplication and coverage acknowledgement before clearing an enforced failure.
