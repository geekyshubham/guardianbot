# Compromised bridge

Disable its registry route, rotate bridge authentication, block its network path,
and mark affected AI reviews untrusted. Models have no GitHub credentials, so
deterministic workflows continue. Audit request IDs and classifications without
exporting repository text; restore only after conformance and isolation checks.
