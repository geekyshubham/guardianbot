# False positive

Record scanner rule, evidence, fingerprint, repository/commit, and technical
rationale. Prefer fixing/tuning the central rule. A temporary suppression requires
owner, ticket, reason, and expiry. AI false positives are dismissed as feedback
and never alter deterministic policy.
