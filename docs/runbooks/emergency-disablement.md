# Emergency disablement

Stop the control-plane service or suspend GitHub App access. Preserve PostgreSQL,
logs, and artifacts. Pinned deterministic repository workflows remain independent;
change branch rules only through an audited operator action. Communicate scope,
reason, start/end time, and recovery validation.
