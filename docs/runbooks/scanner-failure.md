# Scanner failure

Confirm the evidence artifact is absent/malformed, record run and commit IDs, and
keep the gate failed. Retry once at the same immutable workflow SHA. If the central
tool image is unavailable, pause enforcement through an audited ruleset change;
never substitute AI output. Restore and reconcile missed runs.
