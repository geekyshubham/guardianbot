# Generated by guardianctl. Scanner logic remains in Geekyshubham/guardianbot.
name: GuardianBot

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: ["main"]
  schedule:
    - cron: "23 2 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guardianbot-security-gate:
    name: guardianbot/security-gate
    permissions:
      contents: read
      security-events: write
      actions: read
    uses: Geekyshubham/guardianbot/.github/workflows/reusable-security.yml@323344d998e7a3f8d2a828efc212a036650810c2
    with:
      config-path: .guardianbot/config.yml
